apiVersion: v1
kind: Service
metadata:
  name: {{ include "meet-helm-chart.fullname" . }}
  labels:
    {{- include "meet-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.BeService.type }}
  ports:
    - port: {{ .Values.BeService.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meet-helm-chart.selectorLabels" . | nindent 4 }}
