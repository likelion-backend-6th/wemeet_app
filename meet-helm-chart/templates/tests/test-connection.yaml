apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meet-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "meet-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "meet-helm-chart.fullname" . }}:{{ .Values.BeService.port }}']
  restartPolicy: Never
